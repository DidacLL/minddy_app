import {MinddyManager} from "../../../data/Minddy.manager";

interface ProjectNotesProps {
    manager: MinddyManager
}

export function ProjectNotes({manager}: ProjectNotesProps) {
    return null;
}